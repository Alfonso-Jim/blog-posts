import { combineReducers } from "redux";

export default combineReducers({
  replaceMe: () =>
    "This line tricks redux into thinking that it has a valid reducer, remember to change this line.",
});
